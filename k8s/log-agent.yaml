apiVersion: v1
kind: Namespace
metadata:
  name: erp-log-agent
  labels:
    name: erp-log-agent

---

apiVersion: apps/v1
kind: DaemonSet
metadata:
  namespace: erp-log-agent
  name: erp-log-agent
  labels:
    app: erp-log-agent
spec:
  selector:
    matchLabels:
      app: erp-log-agent
  template:
    metadata:
      labels:
        app: erp-log-agent
    spec:
      containers:
        - name: erp-log-agent
          image: saichler/erp-log-agent:latest
          imagePullPolicy: Always
          env:
            - name: LOGPATH
              value: /data/logs/erp
            - name: LOGFILE
              value: "*"
            - name: NODE_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP

          volumeMounts:
            - name: hdata
              mountPath: /data
      volumes:
        - name: hdata
          hostPath:
            path: /data
            type: DirectoryOrCreate